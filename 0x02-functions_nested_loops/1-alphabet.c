#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
void print_alphabet(void);
for (char letter = 'a'; letter <= 'z'; letter++)
		putchar(letter);

	putchar('\n');

int main(void)
{

    print_alphabet();
    return (0);
}

